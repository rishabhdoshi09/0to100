"""Canonical QuantTerm product API package.

External launchers and integrations should depend on :mod:`api.app` rather than
historical implementation module names.  The package is deliberately thin: it
owns the public seam, not a second application or trading authority.
"""
